spring:
  jpa:
    open-in-view: false
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/musifindme
    password: postgres
    username: postgres
    driver-class-name: org.postgresql.Driver
  flyway:
    url: jdbc:postgresql://localhost:5432/musifindme
    user: postgres
    password: postgres
server:
  port: 8081

kachinsky:
  app:
    jwtCookieName: kachinsky
    jwtSecret: ======================likesisterraysaid=========================
    jwtExpirationMs: 86400000


